# Web application layout

`components/`, `dashboard/`, `canvas/`, and `api/` reserve separable UI concerns.
Корневые `index.html` и `style.css` участника проекта остаются неизменными и являются текущей совместимой поверхностью интерфейса.
