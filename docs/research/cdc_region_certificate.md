# Candidate region certificate

The deterministic Study-0 certificate records study identity; exact region bounds and binary64 representation; formula/radius/iteration bound; initial enclosure `z0={0}`; lower bound `Re(z2)>=95/16`; guard identity; conventional escape theorem; potential `1 -> 0`; terminal region claim; and the preserved prior direct-orbit decision. There are no child obligations.

These are separate required claims: (1) enclosure valid; (2) conventional escape predicate true; (3) guard sound; (4) CDC token transition certified; (5) obligation discharged. Validation rejects any false or mismatched field. The CDC rule is Definition 5.2 token descent with `delta=1`; proof-sheet organization is §23.1. The conventional theorem is the strict radius escape theorem. Canonical JSON and the constant study identity provide deterministic identity; repository/source integrity continues to use the PDF SHA-256 in the manifest rather than pretending it is certificate evidence.

This certificate supports exactly one construction transition. It neither certifies orbit advancement nor generalizes interval arithmetic, subdivision, boundedness, or proof search.
