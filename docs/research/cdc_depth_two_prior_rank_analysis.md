# Prior rank composability

## Finding: `LOCALLY_VALID_BUT_NONCOMPOSABLE`

The preserved one-level certificate assigned `rho(R)=1` and `rho(A)=rho(B)=0`. Its exact transition `{1}->{0,0}` remains valid under its finite registered order and is now named `SUPPORTED_ONE_LEVEL_BRANCHING_DESCENT`. It cannot refine A: no nonnegative rank below zero exists for C or D. Changing R to 2 and A/B to 1 inside that already-issued certificate would be retrospective reassignment and is forbidden.

Depth two therefore uses a new certificate and local rule, with ranks assigned when its obligations are created. This extends the *geometric split pattern*, not the old rank certificate. The old result remains a valid local certificate and a concrete demonstration that local validity does not imply composability. Direct orbit advancement remains `NEGATIVE_RESULT`; the object of descent remains `(region, unresolved proof obligation, verified evidence)`.
