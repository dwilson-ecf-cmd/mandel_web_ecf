# Two-child branching decision

## Source passages used

The source supplies an abstract nonnegative potential and guarded token/factor steps (Definitions 5.1–5.2, printed p.12), finite token and factor bounds (Lemmas 5.3–5.4, p.12), and composition only after component certificates (Theorem 5.5, p.12). Guarded choice chooses enabled rules but supplies no descent (Definition 20.1, p.65). Parallel batches require disjoint writes or commuting updates (Definition 20.2, p.65). Amortization requires a nonnegative bank and a proved combined drop (Definition 22.4, p.69). The proof sheet requires state, potential, guards, updates, certificates, halt, invariants, parallel safety, schedule, and bound (§23.1, p.71). Bisection halves width only with continuity and sign-change premises (Example 16.13, p.49). The PDF gives no fractal subdivision rule, finite-branching theorem, or multiset semantics; the explicit multiset definition and ternary encoding here are external mathematics and a narrow engineering interpretation.

## Conclusion: `SUPPORTED_ONE_LEVEL_BRANCHING_DESCENT`

| Element | Candidate | Evidence | Status |
|---|---|---|---|
| Parent state | composite obligation/evidence state | deterministic `R`, active claim, potential 3 | supported |
| Child states | two typed child obligations | canonical identities; independent evidence slots | supported |
| Split rule | exact registered real bisection | validity, cover, rank, token checks | supported narrowly |
| Base order | finite dyadic area rank `{0<1}` | exact `2 -> 1,1` area units | supported narrowly |
| Multiset order | finite replacement extension; ternary weight | definition and `3 -> 2` check | supported narrowly |
| Coverage proof | half-open claim ownership | exact endpoints and matching bounds | supported conventionally |
| Child discharge | two separate escape guards | `95/16>2`, `207/32>2` | supported mixed transitions |
| Terminal proof | empty obligations + evidence + coverage | sequential and batch traces | supported |

A fully specified, non-circular, well-founded progress model exists for this exact split and both split/discharge paths certify. The rank quantum and maximum branching factor are registered before guard outcomes and do not encode which child discharges first. This does not establish general recursive proof-tree construction: geometry is connected only to a semantics-preserving decomposition rule, while conventional mathematics still owns coverage and escape truth and CDC owns only certified obligation transformations.

Direct orbit advancement remains `NEGATIVE_RESULT`; single-obligation discharge and `COMPOSITE_OBJECT_REQUIRED` remain authoritative. Limitations include one fixed-depth split, binary64 dyadics, escape only, no arbitrary regions, boundedness, recursion, scheduling, rendering, arbitrary precision, or performance result. Recommended next study: an independently specified depth-two case that tests whether ranks and branch bounds compose without pre-registering the future leaf layout; reject generalization if a uniform local rule and terminal semantics cannot be proved.
